import React from "react";

const Xscroller = () => {
	return (
        <div style={{height: "4rem", width: "100%", backgroundColor: "red", display: "flex", justifyContent: "space-around", alignItems: "center", overflowX: "scroll"}}>
            
        </div>
    )
};

export default Xscroller;
